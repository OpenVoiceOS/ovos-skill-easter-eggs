# Specify resources to test here.

# Specify languages to be tested
languages:
  - "en-US"

regex: []

# vocab is lowercase .voc file basenames
vocab: []

# dialog is .dialog file basenames (case-sensitive)
dialog:
  - bad_file
  - languages
  - pod
  - rock_paper_scissors_lizard_spock
  - rule0
  - rule1
  - rule2
  - rule3
  - stardate
  - santa

intents:
  # Padatious intents are the `.intent` file names
  padatious:
    - law_of_robotics.intent
    - stardate_intent.intent
    - pod_bay_doors_intent.intent
    - rock_paper_scissors_lizard_spock_intent.intent
    - languages_you_speak_intent.intent
    - portal_intent.intent
    - hal_intent.intent
    - duke_nukem_intent.intent
    - arnold_intent.intent
    - bender_intent.intent
    - glados_intent.intent
    - adult_mode_intent.intent
    - grandma_mode_intent.intent
    - conan_intent.intent
    - bill_and_ted_intent.intent
    - malibu_stacey_intent.intent
    - sing_intent.intent

  # Adapt intents are the names passed to `IntentBuilder`
  adapt: []
